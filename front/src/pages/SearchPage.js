import React from 'react';
import SearchContainer from '../container/landing/SearchContainer'
import HeaderContainer from '../container/common/HeaderContainer';

const SearchPage = () => {
	return (
		<div>
			<HeaderContainer />
			<SearchContainer />
		</div>
	);
};

export default SearchPage;