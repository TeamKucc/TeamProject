import React, { useEffect } from 'react';
import HeaderContainer from '../container/common/HeaderContainer';
import MainContainer from '../container/landing/MainContainer';

const MainPage = () => {
  return (
    <div>
      <HeaderContainer />
      <MainContainer />
    </div>
  );
};

export default MainPage;
