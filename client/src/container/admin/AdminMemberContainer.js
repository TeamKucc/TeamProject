import React,{useEffect} from 'react';
import {useSelector,useDispatch} from 'react-redux'

const AdminMemberContainer =()=>{
    const disPatch = useDispatch()
    const {product,user}=useSelector(({}))
    return(
        <>
        test!!!!!!!!!
        </>
    )
}

export default AdminMemberContainer